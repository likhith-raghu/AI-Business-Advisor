{
  "name": "ai-business-advisor",
  "version": "1.0.0",
  "description": "AI Business Advisor Application",
  "scripts": {
    "start": "concurrently \"cd backend && npm start\" \"cd frontend && npm start\"",
    "install-deps": "npm install && cd frontend && npm install && cd ../backend && npm install"
  },
  "dependencies": {
    "concurrently": "^8.2.0"
  }
}